# このリポジトリを触る前に

No転記は、**他人の Discord チャンネルと、他人のスプレッドシート**を相手にする。
こちらの都合で壊せない場所に書き込むので、機能を足すより「静かで、嘘をつかない」ことを優先する。

以下は非交渉のルール。どれも**実際に破って痛い目を見た**ものだけを載せている。

---

## 1. 判定を足す前に、実応答での発火率を測る

多数派に付く印は、情報量がゼロになる。読む人の負担にしかならない。

```
node -e "..." で test/fixtures/bundle-response.json に新しい判定を当てる
```

`test/fixtures/bundle-response.json` は `gpt-oss-120b` が実際に10件束ねに返した応答そのもの。
新しい検査を書いたら、まずこれに当てて**何件で発火するか数える**。

> **破ったとき：** 「summary が原文の部分文字列か」を検査して `⚠️` に落とす仕組みを入れたら、
> 実応答10件のうち**8件が助詞の違いで発火**した。⚠️ が多数派になり、シートが原文だらけになった。
> 同じ失敗は v3 でも起きている（「補完あり」の印が全行に付いた）。**2回目だった。**

判定を足すのが正しい場面もある。`detectLeak`（他の報告の語の混入）は発火率がほぼ0で、
発火したときは本当に事故なので残してある。**発火率が判断材料。**

## 2. `✅` は「シートに入った」という契約

書けていないのに付けたら嘘になる。書き出しに失敗したら、リアクションを付けず、カーソルも進めない。
月間カウンタだけは保存する（さくらは既に消費しているので、数えないと帳尻が合わない）。

> **破ったとき：** 列ずれで書き込みが失敗しているのに ✅ を付けていた。

## 3. 公開リポジトリ。ログに本文・報告者名・要約を出さない

Actions のログは誰でも読める。出すのは件数と内訳だけ（`投稿2件 → 2行：✅2 ❓0 ⚠️0`）。
ログに出す文字列は必ず `mask()` を通す。環境変数の実値だけでなく、**秘密の形状**も潰す。

## 4. `原文` は必ず残す。要約で上書きしない

`原文` 列（`COLUMNS` の `raw`）は最後の砦。ここさえ残っていれば、間違いは復元可能な間違いになる。

> **効いたとき：** 「クレジットを表記してほしい」が「やめてほしい」に反転していたのを、
> `内容` と `原文` を並べて見つけた。要約だけ残す作りだったら誰も気付けなかった。

## 5. LLM に文章を作らせない

`summary` は**要約ではなく抜き書き**。原文の該当箇所を、語を変えずにそのまま写させる。

> **破ったとき：** 要約させていたら、依頼の述語（〜してほしい）が圧縮で落ち、
> 文脈から否定側が補完されて**意味が正反対**になった。構文的には正常な出力なので検査も素通りする。

プロンプトから「要点」「〜字以内」「言い換える」を消す。**縮めろと言うとモデルは言い換える。**
長さの制限は実装側で切る。

## 6. 本文だけで決まらないことは、モデルに聞かない

種別のような選択肢は、**その現場を知らない人が本文だけを読んで選べる**場合にだけ置く。
選べないなら `CATEGORIES` を1語にする（1語なら実装がプロンプトから設問ごと外す）。

> **破ったとき：** 7語の語彙を持たせていたが、実ログはこうだった。
> ```
> 体験版データ
> ゲ制デーツイート
> 赤い水がすごい速度で淀みが溜まるんですけど、サイトの攻略情報に足しておこう
> ```
> 名詞だけの投稿は不具合ともやることとも読め、3つ目は1文に両方入っている。
> **決めているのは文面ではなく書いた本人の意図**なので、語彙を減らしても当たらない。
> しかも語彙の中で間違えた場合は `種別補完` の印すら付かない（語彙外のときしか付かない）ので、
> 無印の誤りとして積まれる。

「語彙を増やして精度を上げる」は逆方向。**選べないなら選ばせない。**

## 7. 判断に迷ったら捨てない

「報告ではない」（`notreport`）は、**確実にあいさつ・相づちのときだけ**。
少しでも話題に関わる中身があれば拾う。捨てる判断だけ保守的に倒す、という非対称なルール。

> **破ったとき：** システムプロンプトに「業務報告の抽出器です」と決め打ちしていたので、
> 話題が違う投稿を全部 `notreport` で捨てていた。集める対象は `TOPIC` で差し替える。

## 8. 列の骨格を固定しない

列名と値の出どころは `COLUMNS` で対にして指定する（`ID:autoid,種別:category,…`）。
**Bot が書かない列**（`blank`）を持てることが、既存の表に混ぜてもらうための条件。

> **破ったとき：** `記録日時/報告日/報告者/…` の並びを固定していて、
> 転記先の既存表（ID が先頭、Discordリンクが中ほど）に合わせられなかった。

## 9. Discord の ID は必ず文字列で扱う

Snowflake は 64bit。`Number` にすると精度が壊れる。比較は `cmpId()`（桁数 → 辞書順）。

> **破ったとき：** テストで `{ 1534531074158235731: 3 }` と裸の数値をキーにして、`…500` に丸められた。

## 10. シートの行を手で直すときは、位置ではなく `メッセージID` で特定する

詳しい手順は README の「間違った行を直すとき」。

> **破ったとき：** 画面上の座標で行を指定して直したら、確定後にグリッドが1行スクロールし、
> **隣の行の内容を上書き**した。`Ctrl+Z` で戻せたが、気付かなければ他人の報告が1件消えていた。

---

## 動かし方

```
node test/harness.mjs      # 外部通信なし・消費ゼロ
node src/index.mjs --check # 自己診断（さくらは呼ばない）
node src/index.mjs --dry-run
```

依存パッケージは無い（Node 20+ の組み込み `fetch` のみ）。
マーケットプレイスのアクションも使わない（配布物として外部依存を持たないため）。
